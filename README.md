# Lilia Test Server

Простой Go-сервер с одним POST эндпоинтом.

## Запуск

Сервер запустится на `http://localhost:8081`. Порт 8081

### Docker

Сборка образа:

```bash
docker build -t lilia-server .
```

Запуск контейнера:

```bash
docker run -p 8081:8081 lilia-server
```

## API

### GET /

Проверка работоспособности сервера.

### POST /api/hello

Принимает POST-запрос и возвращает приветствие.


# Задание
## На каждый пункт сделай скриншот

1. Спулить репозиторий 
2. Запустить приложение с main ветки и отправить POST запрос
3. Разработчик добавил новую фичу ветке feature/logging - добавлено логирование. Нужно собрать приложение и проверить пишутся ли логи после ПОСТ запроса. Для этого нужно использовать специальную докер команду
4. После тестирования заверши работу докер приложения (команда)
5. Запусти остановленное приложение заново (команда)

